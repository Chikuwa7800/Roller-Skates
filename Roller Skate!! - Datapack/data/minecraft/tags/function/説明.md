# minecraft/tags/function
 ## load.json
  ワールドに入った時又は/reload実行時にのみ実行される関数(function)を定義するファイル。

  基本的にスコアボードの追加に使われる。

  コマンドブロックで言うとインパルスの状態。
 ## tick.json
  常に1tick(1/20秒)ごとに実行される関数(function)を定義するファイル。

  便利だが、一度に大量のコマンドを実行しすぎると重くなるのでファイル分けで最適化をすることを勧める。
  
  コマンドブロックで言うとリピートの状態。